import type { Clue } from '../types/clues';

export const clues: Record<string, Clue> = {
  'first-observation': {
    id: 'first-observation',
    title: 'First Observation',
    description: 'The room feels untouched, but something is wrong.',
  },
};
